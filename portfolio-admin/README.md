# Portfolio Admin Panel

A professional full-stack admin panel for managing a personal portfolio.

## Structure
- `client/`: React frontend.
- `server/`: Node/Express backend with MongoDB.

## Setup
1. **Server**: `cd server && npm install && npm start`
2. **Client**: `cd client && npm install && npm run dev`
